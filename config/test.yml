model:
  backbone: resnet50
  pooling: gem_pooling
  head: BNHead
  bn_where: after
  batch_norm_bias: True

data:
  name: ppe_two
  batch_size: 32

loss:
  name: CEL_Sigmoid

freeze:
  enable: True
  layers: [backbone]
  epochs: 10

lr_scheduler:
  enable: True
  name: WarmupCosineAnnealingLR

trainer:
  epochs: 120
  
trainer_colab:
  epochs: 120